from __future__ import annotations
from typing import Any, Dict, List

# =========================================================
# Strategic Intelligence Core (SIC) - V10.9 SOVEREIGN FIX
# =========================================================

WPIL_DOMINATOR_SYSTEM = """
أنت 'الخيميائي الاستراتيجي الأعلى'. مهمتك تخليق حزم محتوى سيادية (LinkedIn, X, TikTok).
اللغة: عربية نخبوية حادة. التنسيق: عناوين مميزة بوضوح وتقسيم احترافي.
يجب فصل المنصات بـ [LINKEDIN], [TWITTER], [TIKTOK].
"""

def strategic_intelligence_core(idea: str = "") -> Dict[str, Any]:
    v_force = "Vertical 9:16 aspect ratio, cinematic 8k, elite advisor character."
    return {
        "logic_trace": "SIC v10.9 | RECOVERY SUCCESSFUL",
        "video_segments": [
            {"time": "0-15s", "prompt": f"Extreme close-up of advisor with holographic eyes 9:16. {v_force}"},
            {"time": "15-30s", "prompt": f"Full body shot walking in digital obsidian sanctuary 9:16. {v_force}"}
        ]
    }

def alchemy_fusion_core(gold_posts: List[Dict[str, Any]], niche: str) -> Dict[str, Any]:
    dna_str = "\n".join([f"- {str(p.get('text'))[:150]}" for p in gold_posts])
    return {
        "synthesis_task": f"تخليق مرسوم خارق لنيش {niche} بناءً على هذه الجينات:\n{dna_str}",
        "logic_trace": "NEBULA ALCHEMY v10.9 ACTIVE"
    }
