from __future__ import annotations
from typing import Any, Dict, List

# =========================================================
# Strategic Intelligence Core (SIC) - V9.0 SOVEREIGN NUCLEUS
# =========================================================

WPIL_DOMINATOR_SYSTEM = """
أنت 'الخيميائي الاستراتيجي الأعلى'. مهمتك تخليق محتوى مهيمن لـ (LinkedIn, X, TikTok).
القواعد الصارمة:
1. التنسيق: لغة نخبوية، عناوين حادة، مسافات واسعة، تواصل بصري عبر الكلمات.
2. الهيكل: رد واحد يحتوي على المنصات الثلاث مفصولة بـ [LINKEDIN], [TWITTER], [TIKTOK].
"""

def strategic_intelligence_core(idea: str = "") -> Dict[str, Any]:
    v_force = "Vertical 9:16 aspect ratio, cinematic 8k, mobile-first framing, advisor character."
    return {
        "logic_trace": "NEBULA v9.0 | LIVE NERVE ACTIVE",
        "video_segments": [
            {"time": "0-15s", "prompt": f"Close-up portrait of the advisor, 9:16 vertical, cinematic lighting. {v_force}"},
            {"time": "15-30s", "prompt": f"Advisor in high-tech boardroom, 9:16 portrait. {v_force}"}
        ]
    }

def alchemy_fusion_core(gold_posts: List[Dict[str, Any]], niche: str) -> Dict[str, Any]:
    dna_str = "\n".join([f"- {str(p.get('text'))[:150]}" for p in gold_posts])
    return {
        "synthesis_task": f"DNA Samples from market:\n{dna_str}",
        "logic_trace": "ALCHEMICAL FUSION ACTIVE | LIVE DATA"
    }
